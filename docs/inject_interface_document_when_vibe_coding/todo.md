# 注意
> 1. 需要在调用注入接口时，有一些信息标识当前事件的哪些字段需要在服务端校准
> 2. 比如有一个场景，在调用 pressedButton 前，没有调用过moveTo接口，那么就应该在服务端将当前的鼠标光标位置设置到该注入的事件上
> 3. 假设用户先调用接口将光标移动到A，后续移动物理鼠标使得光标到B，此时调用ButtonDown，应该在哪个坐标点呢？
> 4. ButtonDown 的位置是当前设备上光标的位置，而不是Controller上维护的坐标，换句话说，ButtonDown和Axis相关的事件只关注当前设备上的光标位置，而不是Controller上的光标位置
> 